#pragma once

#include "RPrse.hpp"
#include "RKey.hpp"

void Parser::AssembleAST(std::vector<Token> Tokens){
    Tokens.clear();
    ParentNodes* CurrentNode = nullptr;

    for (Token : Token Tokens){
        ;
    }
}
